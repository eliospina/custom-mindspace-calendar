// Copy this file to config.js and add your Google Cloud credentials.
// config.js is gitignored and is not committed to the repo.
window.GOOGLE_CONFIG = {
    CLIENT_ID: 'YOUR_CLIENT_ID.apps.googleusercontent.com',
    API_KEY: 'YOUR_API_KEY',
    // Optional: Google Group join URL for beta testers (OAuth Test users).
    // Fallback: GitHub issue template if empty.
    BETA_ACCESS_URL: '',
};
